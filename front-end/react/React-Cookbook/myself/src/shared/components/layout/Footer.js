import React from 'react';

const Footer=() => {
        return (
            <footer style={{textAlign:'center'}}>&copy; Codejobs {(new Date()).getFullYear()}</footer>
        );
}

export default Footer;
